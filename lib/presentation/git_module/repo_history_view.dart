import 'package:flutter/material.dart';

class RepoHistoryView extends StatelessWidget {
  const RepoHistoryView({super.key});

  @override
  Widget build(BuildContext context) {
    // TODO: Mostrar historial real del repo seleccionado
    return const Center(
      child: Text('Selecciona un repositorio para ver su historial.'),
    );
  }
}
